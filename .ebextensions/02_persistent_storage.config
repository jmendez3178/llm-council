# Mount EFS or use /tmp for SQLite — keeps data across deploys
option_settings:
  aws:elasticbeanstalk:application:environment:
    DATA_DIR: "/var/app/current/data"

commands:
  01_create_data_dir:
    command: "mkdir -p /var/app/current/data"
    ignoreErrors: true
